{
  "type": "object",
  "title": "Evidence Section Output",
  "description": "Structured output schema for the evidence section content extraction. Typed unit arrays: measures, experiment_setups, findings. Valid unit types: Measure, ExperimentSetup, Finding. Authors relations.",
  "required": [
    "section"
  ],
  "additionalProperties": false,
  "properties": {
    "section": {
      "type": "object",
      "required": [
        "section_type",
        "anchor_id",
        "measures",
        "experiment_setups",
        "findings",
        "relations"
      ],
      "additionalProperties": false,
      "properties": {
        "section_type": {
          "type": "string",
          "enum": [
            "evidence"
          ]
        },
        "anchor_id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*:[a-z0-9_]+$",
          "description": "ID of the unit that anchors this section; it must be defined in one typed unit array"
        },
        "measures": {
          "type": "array",
          "description": "Measure units extracted for this section",
          "items": {
            "type": "object",
            "required": [
              "id",
              "type",
              "provenance",
              "name",
              "unit",
              "setup_ids",
              "scores"
            ],
            "additionalProperties": false,
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^[a-z][a-z0-9_]*:[a-z0-9_]+$",
                "description": "Unit identifier in lowercase prefix:descriptor format"
              },
              "type": {
                "type": "string",
                "enum": [
                  "Measure"
                ],
                "description": "Unit type discriminator"
              },
              "provenance": {
                "type": "array",
                "description": "Location markers for this unit: top-level section anchors such as ['\\u00a71', '\\u00a72']",
                "items": {
                  "type": "string"
                }
              },
              "name": {
                "type": "string",
                "description": "Name of the measure/metric"
              },
              "unit": {
                "type": "string",
                "description": "Non-empty measurement unit such as %, ms, BLEU, F1, perplexity, or unitless"
              },
              "setup_ids": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "IDs of local ExperimentSetup units scoping this measure; empty when none apply"
              },
              "comparison_direction": {
                "type": "string",
                "enum": [
                  "higher_is_better",
                  "lower_is_better",
                  "target",
                  "unspecified"
                ],
                "description": "Whether higher or lower values are preferred; omit when unspecified"
              },
              "objective_class": {
                "type": "string",
                "enum": [
                  "primary_quality",
                  "cost_efficiency",
                  "fairness",
                  "safety",
                  "robustness"
                ],
                "description": "Optional (FG-6): which axis of a multi-objective evaluation this measure sits on \u2014 primary_quality (the headline quality metric, the default \u2014 omit), cost_efficiency (latency/compute/memory/params), fairness, safety, or robustness. Set it on the non-primary axes of a trade-off so a cost/fairness/safety measure is not read as uniformly positive evidence."
              },
              "scores": {
                "type": "array",
                "items": {
                  "type": "object",
                  "required": [
                    "variant",
                    "value",
                    "variance",
                    "system_id",
                    "setup_id"
                  ],
                  "additionalProperties": false,
                  "properties": {
                    "variant": {
                      "type": "string",
                      "description": "System name for this score row as the paper labels it \u2014 a method-family variant/configuration or a compared-against baseline (e.g. 'Transformer (big)', 'GNMT')"
                    },
                    "value": {
                      "type": "string",
                      "description": "Reported score encoded as a string, including numeric values"
                    },
                    "variance": {
                      "type": "string",
                      "description": "Uncertainty (e.g. '\u00b1 0.3'); empty string when unreported"
                    },
                    "system_id": {
                      "type": "string",
                      "description": "ID of the Method unit this row reports \u2014 the contribution variant or the compared-against baseline (e.g. 'mth:transformer', 'mth:gnmt'). Empty string when no node represents this row's system (e.g. an ensemble-of-baselines the census did not capture)."
                    },
                    "setup_id": {
                      "type": "string",
                      "description": "ID of the local ExperimentSetup unit this row was measured under \u2014 the dataset/split/protocol (e.g. 'exp:wmt14_ende'). This is how the row binds to its data. Empty string when the measure's own setup_ids already scope every row."
                    },
                    "value_kind": {
                      "type": "string",
                      "enum": [
                        "numeric",
                        "symbolic",
                        "asymptotic",
                        "qualitative",
                        "curve"
                      ],
                      "description": "How to read `value`: numeric (a number, the default \u2014 omit the field), symbolic (a closed-form expression like '2/\u0394\u00b7logT'), asymptotic (a complexity class like 'O(n^6)' or 'PSPACE-complete'), qualitative (a categorical verdict), or curve (a trend). Omit for an ordinary numeric leaderboard score."
                    },
                    "opponent_id": {
                      "type": "string",
                      "description": "Optional (FG-6): for a pairwise/win-rate row (A-vs-B), the id of the Method this row's system was compared against \u2014 `system_id` is system A, `opponent_id` is system B, `value` is A's win rate vs B. Omit for an ordinary absolute-score row."
                    },
                    "judge_id": {
                      "type": "string",
                      "description": "Optional (FG-6): for a judged row (LLM-as-judge or human evaluation), the id of the judge \u2014 a Method, or an inference_protocol/population ExperimentSetup that was materialized for the evaluator. Omit when the score needs no judge."
                    }
                  }
                },
                "description": "Flat array of reported scores under this measure \u2014 one row per system, covering the method family's own variants and every compared-against baseline"
              }
            }
          }
        },
        "experiment_setups": {
          "type": "array",
          "description": "ExperimentSetup units extracted for this section",
          "items": {
            "type": "object",
            "required": [
              "id",
              "type",
              "provenance",
              "role",
              "name"
            ],
            "additionalProperties": false,
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^[a-z][a-z0-9_]*:[a-z0-9_]+$",
                "description": "Unit identifier in lowercase prefix:descriptor format"
              },
              "type": {
                "type": "string",
                "enum": [
                  "ExperimentSetup"
                ],
                "description": "Unit type discriminator"
              },
              "provenance": {
                "type": "array",
                "description": "Location markers for this unit: top-level section anchors such as ['\\u00a71', '\\u00a72']",
                "items": {
                  "type": "string"
                }
              },
              "role": {
                "type": "string",
                "enum": [
                  "dataset",
                  "benchmark",
                  "task",
                  "theoretical_setting",
                  "structural_class",
                  "contribution_resource",
                  "data_split",
                  "inference_protocol",
                  "training_config",
                  "ensembling",
                  "population"
                ],
                "description": "Which experimental ingredient this is \u2014 a substrate the method is tried on (dataset/benchmark/task) or a configuration that scopes a measure (data_split/inference_protocol/training_config/ensembling/population)"
              },
              "name": {
                "type": "string",
                "description": "Name/label of the setup \u2014 a dataset name, a split label, a configuration name"
              },
              "description": {
                "type": "string",
                "description": "Optional one-sentence prose describing the setup; empty string when the name suffices"
              }
            }
          }
        },
        "findings": {
          "type": "array",
          "description": "Finding units extracted for this section",
          "items": {
            "type": "object",
            "required": [
              "id",
              "type",
              "provenance",
              "role",
              "statement"
            ],
            "additionalProperties": false,
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^[a-z][a-z0-9_]*:[a-z0-9_]+$",
                "description": "Unit identifier in lowercase prefix:descriptor format"
              },
              "type": {
                "type": "string",
                "enum": [
                  "Finding"
                ],
                "description": "Unit type discriminator"
              },
              "provenance": {
                "type": "array",
                "description": "Location markers for this unit: top-level section anchors such as ['\\u00a71', '\\u00a72']",
                "items": {
                  "type": "string"
                }
              },
              "role": {
                "type": "string",
                "enum": [
                  "descriptive",
                  "mechanistic",
                  "comparative",
                  "modeling",
                  "ablation_finding",
                  "failure_mode",
                  "theorem",
                  "lemma",
                  "bound"
                ],
                "description": "Classification of the finding"
              },
              "statement": {
                "type": "string",
                "description": "The finding as a single declarative sentence"
              },
              "polarity": {
                "type": "string",
                "enum": [
                  "positive",
                  "negative",
                  "neutral",
                  "mixed"
                ],
                "description": "Optional sign of the finding's headline effect from the method/hypothesis's perspective: positive (confirms/improves), negative (refutes/degrades), neutral (no significant effect / a null result), mixed (direction depends on conditions); omit when not applicable"
              },
              "effect_size": {
                "type": "string",
                "description": "Optional magnitude of the effect in the paper's own terms (e.g. '+2.1 BLEU', '3 orders of magnitude faster', 'r=0.83'); omit when none is stated"
              },
              "scope": {
                "type": "string",
                "description": "Optional conditions/range under which the finding holds (e.g. 'on 11 of 12 tasks', 'in low-resource regimes'); omit when unrestricted"
              }
            }
          }
        },
        "relations": {
          "type": "array",
          "description": "Edges this evidence section authors. Lifted into the global relations[] during assembly. Empty array when none apply.",
          "items": {
            "type": "object",
            "required": [
              "source_id",
              "relation",
              "target_id",
              "provenance"
            ],
            "additionalProperties": false,
            "properties": {
              "source_id": {
                "type": "string",
                "pattern": "^[a-z][a-z0-9_]*:[a-z0-9_]+$",
                "description": "Source unit ID"
              },
              "relation": {
                "type": "string",
                "enum": [
                  "about",
                  "supports"
                ],
                "description": "about = a Finding is about a Method/ExperimentSetup/Measure; supports = a Measure, Finding, or Method (a theorem/proof) supports a Finding."
              },
              "target_id": {
                "type": "string",
                "pattern": "^[a-z][a-z0-9_]*:[a-z0-9_]+$",
                "description": "Target unit ID"
              },
              "provenance": {
                "type": "array",
                "description": "Location markers for this unit: top-level section anchors such as ['\\u00a71', '\\u00a72']",
                "items": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    }
  }
}
